stages:
  - test
  - release

variables:
  NODE_ENV: development
  PRE_COMMIT_HOME: ${CI_PROJECT_DIR}/.cache/pre-commit
  # We have to disable sigstore for now since NPM currently only supports
  # provenance when publishing from a gitlab-hosted runner. Hopefully we can
  # enable this in the future once they drop this limitation (which should be
  # possible since PyPI has no such limitation on their use of sigstore).
  NPM_CONFIG_PROVENANCE: "false"

.dev_dependency_job: &dev_dependency_job
  image: node:24@sha256:377f1c17906eb5a145c34000247faa486bece16386b77eedd5a236335025c2ef
  before_script:
    - npm ci
  cache:
    key: devdependencies
    paths:
      - node_modules

include:
  - component: gitlab.com/thelabnyc/thelab-ci-components/precommit@0.5.1
    inputs:
      image: registry.gitlab.com/thelabnyc/python:3.13.960-node24@sha256:9cb82ee53bc8ad2d7fea12d10f6e3583b6a0924dbaab4bb51b52605396017475
  - component: gitlab.com/thelabnyc/thelab-ci-components/publish-gitlab-release@0.5.1
  - component: gitlab.com/thelabnyc/thelab-ci-components/publish-to-npm@0.5.1

test:
  <<: *dev_dependency_job
  stage: test
  needs: []
  script:
    - npm run check
    - npm run build
    - npm run test
